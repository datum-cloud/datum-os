import { tv } from 'tailwind-variants'

export const dragAndDropStyles = tv({
  slots: {
    container:
      'w-full border border-dashed border-blackberry-500 rounded-[5px] flex items-center justify-center h-[109px]',
  },
})
